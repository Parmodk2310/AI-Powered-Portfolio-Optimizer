target-version = "py310"
line-length = 88

[lint]
# Explicit baseline prevents future Ruff default-rule changes from silently
# redefining the repository quality contract.
select = ["E4", "E7", "E9", "F", "I"]
ignore = []

[lint.per-file-ignores]
"tests/**" = ["F401"]
