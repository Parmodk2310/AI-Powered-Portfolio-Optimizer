# ============================================================
# AI-Powered Portfolio Optimizer — Requirements
# Python 3.9+ required
# Install: pip install -r requirements.txt
# ============================================================

# ── Data Fetching ──────────────────────────────────────────
yfinance==0.2.38               # Stock price history from Yahoo Finance
newsapi-python==0.2.7          # NewsAPI client for financial news
requests==2.31.0               # HTTP requests (used by news_fetcher)
beautifulsoup4==4.12.3         # HTML parsing if scraping fallback needed

# ── Data Processing ───────────────────────────────────────
pandas==2.2.0                  # DataFrames for price/returns data
numpy==1.26.4                  # Numerical operations, matrix math
python-dateutil==2.9.0         # Date parsing utilities

# ── Machine Learning ──────────────────────────────────────
torch==2.2.0                   # PyTorch — required by transformers + FinBERT
torchvision==0.17.0            # Required by some torch pipelines
scikit-learn==1.4.0            # ML utilities, preprocessing

# ── NLP & Transformers ────────────────────────────────────
transformers==4.38.0           # HuggingFace transformers — FinBERT model
sentence-transformers==2.5.1   # Sentence embeddings for FAISS
tokenizers==0.15.2             # Fast tokenizer (dependency of transformers)
huggingface-hub==0.21.3        # Download models from HuggingFace Hub
accelerate==0.27.2             # Faster model loading

# ── Vector Store ──────────────────────────────────────────
faiss-cpu==1.7.4               # FAISS vector similarity search (CPU version)

# ── LLM & RAG ─────────────────────────────────────────────
langchain==0.1.9               # LLM orchestration framework
langchain-openai==0.0.8        # LangChain OpenAI integration
langchain-google-genai==0.0.11 # LangChain Google Gemini integration
langchain-community==0.0.24    # LangChain community integrations
openai==1.12.0                 # OpenAI Python SDK
google-generativeai==0.4.0     # Google Gemini SDK
tiktoken==0.6.0                # Token counting for OpenAI models

# ── Portfolio Optimization ────────────────────────────────
scipy==1.12.0                  # Optimization — Sharpe ratio maximization
cvxpy==1.4.2                   # Convex optimization alternative
statsmodels==0.14.1            # Statistical models, time series

# ── Financial Analysis ────────────────────────────────────
ta==0.11.0                     # Technical analysis indicators
pandas-ta==0.3.14b             # Extended technical analysis

# ── Backend API ───────────────────────────────────────────
fastapi==0.109.2               # Async REST API framework
uvicorn==0.27.1                # ASGI server to run FastAPI
pydantic==2.6.1                # Data validation
pydantic-settings==2.2.1       # Settings management with .env
python-multipart==0.0.9        # File upload support
httpx==0.26.0                  # Async HTTP client
starlette==0.36.3              # ASGI toolkit

# ── Frontend ──────────────────────────────────────────────
streamlit==1.31.1              # Interactive web dashboard
plotly==5.19.0                 # Interactive charts
altair==5.2.0                  # Declarative charts
watchdog==4.0.0                # File watching for auto-reload

# ── Configuration & Environment ───────────────────────────
python-dotenv==1.0.1           # Load .env files
pyyaml==6.0.1                  # YAML config support

# ── Serialization ─────────────────────────────────────────
msgpack==1.0.7                 # Fast serialization

# ── Logging & Monitoring ──────────────────────────────────
loguru==0.7.2                  # Better logging
rich==13.7.0                   # Rich terminal output, progress bars

# ── Testing ───────────────────────────────────────────────
pytest==8.0.1                  # Test framework
pytest-asyncio==0.23.5         # Async test support
pytest-cov==4.1.0              # Coverage reporting

# ── Code Quality ──────────────────────────────────────────
black==24.2.0                  # Code formatter
flake8==7.0.0                  # Linting
isort==5.13.2                  # Import sorting

# ── Jupyter Notebooks ─────────────────────────────────────
jupyter==1.0.0                 # Jupyter notebook
ipykernel==6.29.3              # IPython kernel
ipywidgets==8.1.2              # Interactive widgets
matplotlib==3.8.3              # Plotting in notebooks
seaborn==0.13.2                # Statistical visualization

# ── Utilities ─────────────────────────────────────────────
tqdm==4.66.2                   # Progress bars
tenacity==8.2.3                # Retry logic for API calls
cachetools==5.3.3              # Caching utilities
pytz==2024.1                   # Timezone handling